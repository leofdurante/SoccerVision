# SoccerVision frontend

Next.js + TypeScript + Tailwind dashboard for SoccerVision. See the
[repo root README](../README.md) for what this project does, how the
frontend and backend fit together, and how to run everything locally.

Quick start (backend must already be running on :8000):

```bash
npm install
cp .env.example .env.local
npm run dev
```
